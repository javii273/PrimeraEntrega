using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Script : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log ("En esta escena se han usado un cilindro, una esfera y una capsula, objetos 3D basicos, y dos arboles procedentes de la Asset Store")
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
